<style>

  /* Search Button */
  .search-btn {
    background-color: #fe424d;
    color: white;
    border-radius: 25px;
    padding: 0 2em 0 2srem;
    border: none;
  }

  .search-btn:hover {
    background-color: #e63946; /* Slightly darker hover */
  }

  .search-btn i {
    margin-right: 0.5rem;
  }

  /* Search Input */
  .search-inp {
    border-radius: 25px;
    padding: 0.5rem 2rem;
    font-size: 1rem;
    width: 100%; /* Ensure proper scaling */
  }

</style>

<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="/listings">
        <img src="/images/logo1.png" alt="Logo" style="height: 50px;">
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" >
        <span class="navbar-toggler-icon"></span>
      </button>
     <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-link" href="/listings">Explore</a>
        </div>
        
        <div class="navbar-nav">
          <a class="nav-link" href="/bookings/my-bookings">My Bookings</a>
        </div>
        <div class="navbar-nav d-flex ms-auto">
          <form class="d-flex" action="/listings/search" method="GET" >
            <input class="form-control search-inp me-2" type="search" name="q" placeholder="Search destinations" required>
            <button class="btn search-btn" type="submit">
              <i class="fa-solid fa-magnifying-glass"></i> Search
             </button>
          </form>
        </div>
        <div class="navbar-nav ms-auto">
          <a class="nav-link" href="/listings/new">Add your Home</a>
          <% if(!currUser) {%>
          <a class="nav-link"  href="/signup"><b>Sign up</b></a>
          <a class="nav-link" href="/login"><b>Log in</b></a>
          <% } %>
          <% if(currUser) {%>
          <a class="nav-link" href="/logout"><b>log out</b></a>
          <% } %>
        </div>
      </div>
    </div>
  </nav>